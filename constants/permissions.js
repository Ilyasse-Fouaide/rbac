const SYSTEM_PERMISSIONS = {
  // Administrator
  ADMINISTRATOR: 'administrator',
};

/**
 * After adding a permission
 * go to `../services/roleService.js`
 * and add your permission to initializeSystemPermissions() funtion
 */

module.exports = { SYSTEM_PERMISSIONS };
